# Décisions — Jolof Stream

Format : Décision | Raison | Date

---

D-001 | Stack : Next.js 14 + TypeScript + Supabase + Prisma + Vercel | Défini dans le CDC V1.0 | Mai 2026
D-002 | Auth : NextAuth.js, session JWT 7 jours, 2 comptes admin à droits égaux | Défini dans le CDC section 3 | Mai 2026
D-003 | Pas de paiement en ligne direct sur la plateforme — paiement via Wave Business hors site | Défini dans le CDC section 7 | Mai 2026
D-004 | PDF : génération via React-PDF ou PDFKit côté serveur | Défini dans le CDC section 11.3 | Mai 2026
D-005 | Emails : Resend pour les emails automatiques et le mail marketing | Défini dans le CDC section 13.2 | Mai 2026
D-006 | Nomenclature devis : DEV-AAAA-JS-XXX — Nomenclature factures : FAC-AAAA-JS-XXX | Défini dans le CDC section 12.1 | Mai 2026
D-007 | BRS 5% et TVA 18% optionnels par devis/facture — exonération TVA possible par client | Défini dans le CDC sections 6.3 et 6.4 | Mai 2026
D-008 | Site public en français uniquement (Phase 1) | CDC ne mentionne pas le wolof en Phase 1 | Mai 2026
D-009 | Phase 1 = MVP complet (site public 6 pages + dashboard tous modules sauf Comptabilité, Contrats, Mail Marketing) | Défini dans le CDC section 14 | Mai 2026
D-010 | Déploiement : Vercel d'abord, connexion domaine jolofstream.com (LWS) ensuite | Défini dans le CDC section 1.1 | Mai 2026
D-011 | PDF : React-PDF retenu (cohérent §13.1, preview temps réel exigée §6.3.2, meilleure intégration React) | Mai 2026
D-012 | Données entreprise manquantes (NINEA, RC, Wave, logo, photos, emails cofondateurs) : placeholders en dev, saisie dans Parametres avant lancement | Mai 2026
D-013 | Dashboard admin restructure en route groups Next.js : app/admin/(auth)/login et app/admin/(dashboard)/* | Empeche la boucle infinie de redirect quand le layout dashboard fait getServerSession sur /admin/login. Les URLs restent inchangees (route groups invisibles dans l'URL). | Mai 2026
D-014 | Topbar : bouton d'action contextuel emet un CustomEvent admin:primary-action plutot qu'un onClick vide | Respecte Regle 1 (aucun bouton sans action) tout en differant la logique a chaque module. Les pages de module ecouteront cet evenement aux Prompts 06+ | Mai 2026
D-015 | Fonts : system font stack (font-sans Tailwind) au lieu de Google Fonts | Registre Google Fonts inaccessible depuis le conteneur (host_not_allowed). Aucune dependance Google Fonts donc deploiement Vercel sans degradation. Si typographie custom souhaitee plus tard, ajouter localement via next/font/local | Mai 2026
D-016 | Icones reseaux sociaux : SVG inline (FacebookIcon, InstagramIcon, YoutubeIcon, LinkedinIcon dans components/public/social-icons.tsx) | Lucide-react 1.16 a retire toutes les icones de marques pour raisons legales. Pas de remplacement upstream simple. SVG inline minimaux ecrits a la main (paths publics simple-icons style) | Mai 2026
D-017 | Framer Motion 12 : ease "easeOut" annote en as const pour satisfaire le typage strict Variants.transition.ease | Sans as const, TypeScript elargit le type a string et refuse de l'assigner au type Easing. Solution canonique recommandee par Framer pour TS strict | Mai 2026
D-018 | Logos : 3 fichiers PNG dans public/logos/ avec noms exacts Logo_JolofStream_couleur.png / Logo_JolofStream_blancJaune.png / Logo_JolofStream_blanc.png. Composants Logo reutilisables (components/public/logo.tsx et components/admin/logo.tsx) avec fallback onError silencieux (display:none si fichier manquant) | Mai 2026
D-019 | CRM Clients : suppression physique autorisee (pas d'archivage). Clients avec historique : la suppression sera bloquee par les contraintes FK Prisma (projects, quotes, invoices, contracts, marketing_contacts), le message d'erreur dans la route DELETE explicite cette regle a l'utilisateur | Mai 2026
D-020 | Projets : suppression physique uniquement pour statut "prospect". Tous les autres statuts (confirme, en_cours, livre, archive, perdu) sont conserves historiquement (CDC regle metier - les projets Perdus restent avec note de raison, jamais supprimes) | Mai 2026
D-021 | Vue Kanban projets : sans drag-and-drop (hors scope Phase 1). Changement de statut via boutons fleches sur chaque carte et via Select dans la vue tableau. Kanban affiche uniquement les 4 statuts actifs (prospect, confirme, en_cours, livre), les statuts terminaux (livre, archive, perdu) restent visibles en vue tableau | Mai 2026
D-022 | Devis : suppression uniquement si statut brouillon. Devis acceptes/refuses conserves historiquement (CDC regle metier 6.3.2) | Mai 2026
D-023 | Factures : pas de suppression ni modification des lignes apres emission. PATCH limite au statut. Correction via avoir uniquement. Reference avoir : FAC-AAAA-JS-XXX-AVOIR (montants negatifs sur lignes et totaux) | Mai 2026
D-024 | React-PDF : composants PDFViewer et PDFDownloadLink isoles dans components/admin/documents/pdf-preview.tsx et pdf-download.tsx, importes via dynamic({ssr:false}) depuis les consommateurs. Le PdfTemplate (Document/Page/Text/View) reste universel et peut etre importe partout | Mai 2026
D-025 | Compteur sequentiel devis/factures : count() dans une transaction Prisma au moment de la creation. Format AAAA reset chaque annee automatiquement (count() filtre sur startsWith DEV-AAAA-JS- / FAC-AAAA-JS-) | Mai 2026
D-026 | Formations : confirmation paiement = action manuelle admin (bouton "Confirmer paiement"). Aucun webhook Wave. L'admin verifie le paiement recu sur Wave Business puis clique confirmer. Cote serveur, controle anti-overbooking (rejet si la session est deja a maxSeats confirmes). | Mai 2026
D-027 | Liste d'attente : promotion automatique cote serveur dans /api/formations/inscriptions/[id]?action=annuler. Le premier de la liste passe en statut "en_attente" et toutes les positions suivantes sont decremente. Email automatique de notification a implementer au Prompt 12. | Mai 2026
D-028 | Suppression session : autorisee uniquement si aucune inscription confirmee (CDC regle metier : conserver historique). Sinon le statut "annule" doit etre utilise. La suppression efface aussi les inscriptions en attente / liste d'attente associees (cascade applicative dans une transaction Prisma). | Mai 2026
D-029 | Catalogue : modele Offer ajoute au schema Prisma. Pas de migration cote conteneur (DB injoignable) - prisma generate suffit pour le build. Migration `npx prisma db push` a executer en local, puis `prisma db seed` pour les 4 offres par defaut (CEO Essentiel/Premium + Weekend Solo/Collab) | Mai 2026
D-030 | Site public /services et /portfolio connectes a la DB via fetch SSG avec revalidate 60s. Fallback hardcode si DB inaccessible ou liste vide (le service Captation reste 100% hardcode car hors catalogue ; CEO Content et Creator Weekend utilisent les offres DB). Section Portfolio de la page Accueil utilise fetch cote client (useEffect) avec meme fallback | Mai 2026
D-031 | Portfolio dashboard : reordonnancement par boutons fleches haut/bas (swap des displayOrder via 2 PATCH paralleles). Pas de drag-and-drop (hors scope Phase 1). Affichage trie par displayOrder ASC | Mai 2026
D-032 | Parametres : stockes dans la table Setting (cle-valeur). Valeurs structurees (valeurs, equipe, stats, temoignages) serialisees en JSON dans le champ value. Defaults seedees au lancement via PARAM_DEFAULTS dans lib/parametres.ts. GET public, POST authentifie. | Mai 2026
D-033 | Profil admin : chaque cofondateur modifie uniquement son propre profil via /api/profil (PATCH). Email non modifiable. Mot de passe optionnel (laisse vide = inchange) avec validation min 8 + confirmation. Bcrypt 12 rounds. | Mai 2026
D-034 | PdfTemplate etendu avec props entreprise (companyName/Address/Email/Phone/Ninea/Rc/pdfFooterText) avec defaults. Document-form et document-detail injectent ces props via le hook usePdfCompany (fetch /api/parametres au mount). Le PdfTemplate reste rendrable sans props grace aux defauts. | Mai 2026
D-035 | Emails : expediteur "Jolof Stream <onboarding@resend.dev>" par defaut (env EMAIL_FROM override). Domaine custom `notifications@jolofstream.com` necessite verification DNS sur Resend - a configurer Phase 1.5. Lib commune lib/email.ts encapsule resend.emails.send. | Mai 2026
D-036 | Echec email non bloquant : tous les envois sont dans try/catch. Si Resend est indisponible ou si l'envoi echoue, l'action metier (inscription, confirmation, lead, conversion) reussit quand meme. Le warning est loggue cote serveur. | Mai 2026
D-037 | Envoi devis par email = action admin manuelle via POST /api/devis/[id]/envoyer (bouton "Envoyer par email" sur la fiche, visible si statut brouillon ou envoye). Passe le statut a "envoye" si etait brouillon. ActivityLog enregistre l'envoi. | Mai 2026
D-038 | Relance facture = action admin manuelle via POST /api/factures/relances (bouton "Envoyer une relance" sur la fiche, visible si statut emise ou partiellement_payee, hors avoir). Calcule daysPastDue depuis dueAt. ActivityLog. Phase 2 ajoutera la relance automatique via cron. | Mai 2026
D-039 | Vue d'ensemble : KPIs calcules cote serveur a chaque chargement (Server Component avec Promise.all de queries Prisma). Pas de cache (no-cache implicite). Graphique CA : 12 derniers mois glissants, agrege depuis paidAt des factures payees non-avoir. Fallback valeurs a 0 si DB inaccessible. | Mai 2026
D-040 | Taches : suppression uniquement par le createur (champ createdBy). L'autre cofondateur peut modifier (titre, date, statut completed) mais le bouton Trash est desactive avec tooltip explicatif. ActivityLog distingue completion / re-ouverture / modification simple. | Mai 2026
D-041 | SEO : metadataBase pointe vers NEXT_PUBLIC_SITE_URL (defaut https://jolofstream.com). Metadata par page avec template "%s | Jolof Stream". OG image = copie du logo couleur en attendant une vraie image 1200x630px aux couleurs Jolof. Pages legales (CGV, Mentions) en robots noindex/nofollow. | Mai 2026
D-042 | Deploiement Vercel : buildCommand inclut `npx prisma generate && npm run build`. Script `postinstall` dans package.json garantit prisma generate apres chaque `npm install`. vercel.json minimal cree. Headers de securite (X-Frame-Options DENY, X-Content-Type-Options nosniff, Referrer-Policy) dans next.config.js. | Mai 2026
D-043 | Typographie site public : Instrument Serif (display) + Bricolage Grotesque (sans). Charge via CSS @import dans globals.css (pas next/font/google car Google Fonts est host_not_allowed dans le conteneur Claude Code, mais fonctionne en prod sur Vercel + en local). Fallback Georgia/system fonts via font-display: swap. Variables CSS --font-display et --font-sans referenced dans tailwind.config.ts. | Mai 2026
D-044 | Palette site public : tokens ink/cream/red-soft alignes sur le prototype de reference. bg-cream (#FAF8F5) remplace bg-background sur le layout public. ink (#161110), ink-2 (#2E2220), ink-3 (#6B5550), ink-4 (#A8908A), cream-2 (#F2EFE9), cream-3 (#E8E3DC), red-soft (#F7EDED), yellow-light (#FFF3C0) ajoutes dans tailwind.config.ts. | Mai 2026
D-045 | Design system site public : grille portfolio 2fr 1fr 1fr avec row-span-2 sur item 1, cartes services avec barre rouge scaleX au hover, badge date formations en font-display (Instrument Serif) sur fond rouge, hero section avec stats en bas (3 colonnes separees par border-t), Navbar passe en `fixed` (vs sticky) avec mode dark transparent sur hero + cream/blur sur pages claires. PageHero accepte un prop `eyebrow` (badge categorie) + supporte `title` en HTML pour les italiques jaunes. | Mai 2026
D-046 | Comptabilite Phase 2 : recettes = factures status="payee" type!="avoir" (paidAt). Depenses = table Expense (CRUD complet, categorie/montant/date/description/projectId optionnel). Benefice = recettes - depenses (toutes periodes). Marge = (benefice / recettes) * 100 si recettes > 0, sinon 0. Series 12 mois glissants alignes sur paidAt (recettes) et date (depenses). Factures impayees = status in ("emise","partiellement_payee") type!="avoir", jours retard = max(0, today - dueAt). | Mai 2026
D-047 | Comptabilite exports : export CSV client-side via lib/csv-export.ts (separateur ";" pour Excel FR + BOM UTF-8 pour les accents). Trois exports : depenses, recettes, rentabilite par projet. Exports Excel (.xlsx) et PDF reportes en Phase 2.1+ (necessite lib SheetJS / pdfmake non installees). Le snapshot CSV reflete le filtre courant (categorie/periode/client) et la page chargee actuellement. | Mai 2026
D-048 | Export Excel comptabilite : SheetJS (xlsx 0.18.5). 4 feuilles (Resume / Recettes / Depenses / Rentabilite par projet). Header en gras avec fond rouge #C8151B + texte blanc. Format nombre avec separateur milliers sur colonnes montants. Largeurs colonnes auto-ajustees (min 10, max 60). Nom fichier : jolofstream-comptabilite-{YYYY}.xlsx (annee) ou jolofstream-comptabilite-{YYYY}-{MM}.xlsx (mois). Filtre periode par paidAt pour les factures et date pour les depenses. | Mai 2026
D-049 | Export PDF comptabilite : React-PDF renderToBuffer cote serveur (route API Node.js, pas SSR client). 4 pages A4 : Synthese (cartes recettes/depenses/benefice/marge + barre marge), Recettes (tableau avec total), Depenses (tableau + repartition par categorie avec %), Rentabilite par projet (top 3 en fond jaune #FFF3C0 + marqueur asterisque). Header avec logo texte Jolof Stream rouge, footer avec pagination. Charge company_name et company_address depuis Setting avec fallback PARAM_DEFAULTS. | Mai 2026
D-050 | Alertes impayes automatiques : POST /api/comptabilite/alertes. Selection : status in ("emise","partiellement_payee") et type != "avoir" et dueAt < now. Pour chaque facture en retard, envoi Email 7 (relance-facture.tsx) via sendEmail + ActivityLog (UPDATE entityType Invoice). Envoi email non bloquant (try/catch individuel par facture). UI : bouton orange visible uniquement si factures_impayees.count > 0, Dialog de confirmation avant envoi, banner de succes avec compteur apres execution. | Mai 2026
D-051 | Period selector page Comptabilite : 4 valeurs (ce_mois / mois_prec / trimestre / annee). Pilote a la fois les exports (year+month en query) et les 3 tableaux (dateFrom/dateTo). Les selecteurs de periode internes des tableaux ont ete supprimes au profit du selecteur page-level (single source of truth). Trimestre courant = quartier civil (Q1 jan-mars, Q2 avr-juin, etc.). Pour les exports trimestre, on envoie seulement year (le mois est null car le rapport mensuel n'est pas adapte au quartier). | Mai 2026
D-052 | Contrats : suppression physique uniquement si statut "a_envoyer". Contrats envoyes/signes/refuses conserves historiquement. Transitions statut autorisees : a_envoyer -> envoye/annule, envoye -> signe/refuse/annule, refuse -> annule, signe et annule = terminaux. signedAt rempli automatiquement au passage en "signe". | Mai 2026
D-053 | PDF contrat : genere cote serveur via renderToBuffer @react-pdf/renderer. 5 templates (prestation_services, ceo_content, creator_weekend, formation, personnalise) avec 8 articles communs (objet, duree, paiement, obligations prestataire, obligations client, propriete intellectuelle, resiliation, loi applicable). Obligations prestataire variables par template, autres articles communs. Zones de signature double (Pour Jolof Stream / Le Client) en bas de page 2. Notes internes affichees dans encadre jaune si presentes. Reference = 8 derniers caracteres du cuid en uppercase. | Mai 2026
D-054 | Schema Prisma : champ "notes" String? ajoute au modele Contract (necessite npx prisma db push en local). Migration a executer apres deploiement Vercel ou sur l'environnement local. | Mai 2026
D-055 | Email contrat (Email 8) : declenche par POST /api/contrats/[id]/envoyer lors du passage a_envoyer -> envoye. Si le client n'a pas d'email enregistre, le statut passe en envoye sans envoi (UI : bouton "Marquer envoye" au lieu de "Envoyer par email"). Echec d'envoi Resend non bloquant (le statut passe quand meme, l'ActivityLog distingue les 3 cas : envoye OK / envoye sans email delivre / pas d'email client). | Mai 2026
D-056 | Signature PDF Jolof Stream : URL externe stockee dans Setting pdf_signature_url. Apercu image inline dans Parametres > Documents PDF (max 200x100px) avec bouton Effacer. Inseree dans la zone "Pour Jolof Stream" du contrat PDF (page 2) via <Image src=... /> de @react-pdf/renderer si l'URL commence par http(s). Upload Supabase Storage natif reporte Phase 3. | Mai 2026
D-057 | next.config.js remotePatterns : ajout d'un pattern permissif {protocol:"https", hostname:"**"} pour autoriser toute image HTTPS dans next/image (necessaire pour les signatures et apercus admin). A restreindre en Phase 3 si vulnerabilites detectees. | Mai 2026
D-058 | Mail Marketing - contacts : table Prisma MarketingContact (email unique, lists String[]). Sync CRM = upsert idempotent depuis Client.email (avec firstName/lastName extraits du nom complet, clientId rempli, lists initialisees a ["clients"]). Max 500 contacts par import CSV. Parsing PapaParse cote client avec preview 5 lignes avant POST. Email normalise en lowercase trim avant insertion. | Mai 2026
D-059 | Desabonnement : champ unsubscribed dans MarketingContact. Les contacts desabonnes restent en base (historique) mais sont exclus de l'export CSV et des envois futurs. Filtre dedie dans l'UI (case "Afficher uniquement les desabonnes"). Re-abonnement possible via PATCH. ActivityLog enregistre desabonnement et reabonnement. | Mai 2026
D-060 | Listes Marketing : tableau de strings dans MarketingContact.lists (PostgreSQL String[]). 5 listes pre-definies (clients, prospects, formations, newsletter, vip) avec labels et couleurs centralises dans lib/marketing.ts. Listes personnalisees creables au formulaire (normalisees lowercase_with_underscore). API /api/marketing/listes agrege les counts par liste (exclut desabonnes). Sidebar de navigation par liste sur la page Mail Marketing. | Mai 2026
D-061 | Campagnes Mail Marketing : body stocke en HTML brut dans MarketingCampaign.body. Variables {{prenom}}/{{nom}}/{{email}} et autres tokens conserves tels quels, remplaces a l'envoi (Prompt 22). Preview temps reel cote client via iframe srcDoc (debounce 500ms). Template HTML d'enveloppe defini dans lib/campaign-templates.ts (renderCampaignHtml) : header rouge Jolof Stream, body 600px max, footer avec lien desabonnement, style inline pour compatibilite clients email. Statuts : brouillon -> planifie -> envoye (terminal) ; brouillon/planifie peuvent etre annules. PATCH refuse sur status=envoye. DELETE uniquement sur brouillon. | Mai 2026
D-062 | Templates campagnes : 5 templates pre-definis dans lib/campaign-templates.ts (nouvelle_formation, offre_service, newsletter, relance_prospect, remerciement). Selection pre-remplit subject + body. Option "Contenu personnalise" sans template (templateType vide). Variables Liquid-style {{token}} libres dans le body (resolution Prompt 22). Toolbar HTML minimale dans l'editeur (Bold/Italique/Lien/H2/Liste) qui entoure la selection courante du textarea avec les balises HTML correspondantes. | Mai 2026
D-063 | Mail Marketing page : 2 onglets racine (Contacts | Campagnes). Sidebar listes uniquement dans l'onglet Contacts. Topbar admin:primary-action conserve "Nouveau contact" comme action principale ; l'onglet Campagnes a son propre bouton "Nouvelle campagne" pointant vers /admin/mail-marketing/campagnes/nouvelle. | Mai 2026
D-064 | Tracking ouvertures Mail Marketing : pixel GIF 1x1 transparent (R0lGODlhAQABAIAAAAAAAP///yH5BAEAAAAALAAAAAABAAEAAAIBRAA7 base64) servi par /api/marketing/track/open. Email encode en base64 dans l'URL (masquage minimal). Ouvertures multiples comptees (chaque ouverture = nouvelle ligne CampaignOpen). Stat "ouverts_uniques" calculee via groupBy contactEmail. Aucune authentification (appel depuis clients email). Toujours retourner le pixel meme en cas d'erreur DB. Cache-Control: no-store. User-Agent enregistre pour analyse future. | Mai 2026
D-065 | Tracking clics : /api/marketing/track/click GET, parametres campaignId + email (base64) + url (encodeURIComponent). Validation safeUrl : protocole http/https requis, fallback "/" sinon. Redirection 302 vers l'URL d'origine. Insertion CampaignClick. Exclusion : ne pas tracker les liens commencant par {{, #, mailto:, tel:. UserAgent enregistre. | Mai 2026
D-066 | Desabonnement : route GET /api/marketing/unsubscribe accessible publiquement, email en base64 dans l'URL. Met unsubscribed=true sur MarketingContact. Retourne une page HTML de confirmation (style inline) qui affiche l'email desabonne. Idempotent (re-cliquer ne change rien). En cas de lien invalide : page d'erreur explicite avec contact mailto. Le lien est insere automatiquement dans le footer de tous les emails de campagne via renderCampaignHtmlWithTracking. | Mai 2026
D-067 | Nouveaux modeles Prisma CampaignOpen + CampaignClick avec onDelete: Cascade sur campaignId, et @@index([campaignId]) + @@index([contactEmail]) pour acceleration des stats. SQL fourni en annexe du rapport (db push requis en local : Supabase reseau bloque dans le conteneur). | Mai 2026
D-068 | Notifications in-app : modele Prisma Notification (userId / type / title / message / entityType / entityId / entityUrl / read) avec onDelete: Cascade et indexes userId+read. Cloche topbar polling 30s via setInterval cote client (GET /api/notifications?read=false&limit=5). Helpers lib/notifications.ts : createNotification (1 user) et notifyAllAdmins (tous admins via findMany). Branches : POST /api/contact/devis (nouveau_lead), POST /api/formations/inscription (nouvelle_inscription), POST /api/formations/inscriptions/[id] confirmer (paiement_confirme), POST /api/taches creation (tache_assignee, uniquement si assignedTo != createur). Tous appels en try/catch non bloquants. PATCH /api/notifications/[id] verifie userId == session.user.id. | Mai 2026
D-069 | Calendrier : implementation maison (pas de react-big-calendar ni FullCalendar). Sources fusionnees : Project.date (status not in [archive, perdu]), TrainingSession.dateStart, Task.dueDate (completed=false). Vue mois (grille 6 lignes x 7 jours alignes lundi, jour courant en cercle rouge, max 2 pills par cellule + compteur) et vue semaine (7 colonnes verticales avec events listes complets). Sheet lateral pour detail d'un jour. Code couleurs : projet confirme #C8151B, en_cours #F5B800, prospect #6B7280, livre #10B981, formation #8B5CF6, tache normale #3B82F6, tache en retard (dueDate < today) #EF4444. Legende affichee en bas. | Mai 2026
D-070 | Nouvelle table Prisma Notification : SQL fourni en annexe du rapport. db push requis en local (Supabase bloque dans le conteneur). | Mai 2026
D-071 | Upload fichiers Supabase Storage : 4 buckets publics (avatars, signatures, portfolio, equipe). Routes API /api/storage/upload (POST FormData, valide type + taille 5MB, sanitize nom, prefix Date.now()) et /api/storage/delete (DELETE, parse l'URL publique pour extraire le path). Composant ImageUpload reutilisable : 3 ratios (square 96px rond, landscape aspect-video, signature 300x100), drag&drop, loading states, suppression best-effort. Integre dans : Parametres > Mon profil (avatar), Parametres > Documents PDF (signature, remplace l'apercu manuel), Parametres > Contenu > Equipe (photos membres, remplace l'input URL), Portfolio form (photo si mediaType=photo, YouTube URL si mediaType=youtube). Service role utilise cote serveur via supabaseAdmin. | Mai 2026
D-072 | Google Analytics 4 : @next/third-parties (v16.2.6). Composant <GoogleAnalytics gaId={...} /> dans le root layout, monte uniquement si NEXT_PUBLIC_GA_ID est defini. Variable optionnelle ajoutee dans .env.vercel.example. ID a generer dans Google Analytics 4 Admin > Property settings une fois le compte cree. | Mai 2026
D-073 | Sitemap dynamique : app/sitemap.ts en async function avec 2 sources DB (PortfolioItem.published=true et TrainingSession.status=ouvert). Try/catch independant par source pour tolerance : si la DB est inaccessible, le sitemap statique (6 pages) reste servi. Generation au build time par defaut (output ○ static dans next build). | Mai 2026
D-074 | Schema.org JSON-LD : composant components/public/json-ld.tsx (dangerouslySetInnerHTML safe car data controlee cote serveur). 4 schemas ajoutes : Organization (accueil), Service (/services), EducationalOrganization (/formations), LocalBusiness (/contact). Pas de schema FAQPage sur /contact pour eviter conflits avec FAQ accordion (a ajouter en Phase 3 si besoin). | Mai 2026
D-075 | PWA basique : app/manifest.ts genere /manifest.webmanifest (name, short_name, display standalone, theme_color rouge, background_color ink, icone unique logo couleur). Metadata root : manifest URL + appleWebApp + viewport themeColor. Permet "Ajouter a l'ecran d'accueil" sur mobile. Service worker complet (offline, push notifications) reporte Phase 3. | Mai 2026
D-076 | OG image dynamique : app/(public)/opengraph-image.tsx via next/og ImageResponse, runtime edge. 1200x630 px avec fond ink, gradient rouge radial coin haut-droit, logo texte rouge, titre 72px ("Capturez l'instant, diffusez l'emotion" en jaune italic) et sous-titre. Metadata root pointe vers /opengraph-image pour openGraph.images et twitter.images. L'ancienne /og-image.png devient inutilisee. | Mai 2026
D-077 | Header Link preconnect ajoute dans next.config.js : <https://fonts.googleapis.com>; rel=preconnect, <https://fonts.gstatic.com>; rel=preconnect; crossorigin. Optimise le LCP en initiant la connexion TCP/TLS aux serveurs Google Fonts en parallele du HTML, avant que le navigateur ne parse le @import dans globals.css (D-043). Compatible avec tous les navigateurs. | Mai 2026
D-078 | PWA service worker : public/sw.js avec 3 strategies de cache. Cache First pour assets statiques (/_next/static, /logos, extensions .png/.jpg/.webp/.svg/.gif/.ico/.woff/.woff2/.css/.js). Network First avec fallback offline pour pages HTML (text/html). Stale While Revalidate pour API publique (/api/catalogue, /api/portfolio). Exclusions explicites : /admin, /api/auth, /api/notifications, /api/marketing/track, /api/marketing/unsubscribe, /api/storage. Cache name jolofstream-v1, OFFLINE_URL = /offline. skipWaiting + clients.claim a l'install pour mise a jour immediate. Enregistrement uniquement sur site public (composant PwaRegister dans app/(public)/layout.tsx). | Mai 2026
D-079 | Icones PWA : 3 routes Edge runtime (app/icon-192.png/route.tsx, app/icon-512.png/route.tsx, app/apple-icon.png/route.tsx) generees via next/og ImageResponse. Design : fond rouge #C8151B (avec borderRadius 20% pour 192/512 maskable, sans borderRadius pour apple iOS), cercle jaune #F5B800 contenant un triangle play blanc, texte "jolof" blanc bold en dessous. Tailles 192/512/180. Cache-Control: public, max-age=31536000, immutable. Manifest /manifest.webmanifest reference les 3 icones (purpose maskable pour 192, any pour 512). metadata root layout duplique les icones pour les balises <link rel=icon> standards. | Mai 2026
D-080 | Install prompt PWA : composant PwaInstallPrompt (banner bottom-right md / fullwidth mobile, fond ink, accent rouge). Ecoute l'evenement beforeinstallprompt (Chrome/Edge), preventDefault et stocke l'event. Apres delai 3s : affichage du banner. Refus : localStorage pwa-install-dismissed avec date ISO, suppression du banner pour 7 jours. Acceptation : appelle deferredPrompt.prompt() puis lit userChoice. Si display-mode standalone (deja installe) : ne s'affiche pas. iOS Safari ne supporte pas beforeinstallprompt (instruction native "Add to Home Screen" requise). | Mai 2026
D-081 | Web Push API : librairie web-push (v3.6.7) cote serveur. Cles VAPID generees une fois (NEXT_PUBLIC_VAPID_PUBLIC_KEY + VAPID_PRIVATE_KEY + VAPID_SUBJECT). Modele Prisma PushSubscription (userId / endpoint unique / p256dh / auth / userAgent) avec onDelete Cascade et index userId. 3 routes API : /api/push/subscribe POST (upsert by endpoint), /api/push/unsubscribe DELETE (verifie ownership), /api/push/test POST (envoie au user connecte). Service worker public/sw.js etendu avec event listeners push (showNotification depuis JSON payload) et notificationclick (focus fenetre /admin existante ou openWindow). Composant PushSubscribe integre dans Parametres > Mon profil avec 4 etats (loading/unsupported/denied/default/granted) et boutons Activer/Test/Desactiver. | Mai 2026
D-082 | Push non bloquant : sendPushToUser et sendPushToAllAdmins enveloppes dans try/catch silencieux dans lib/notifications.ts. Echec push (Resend indisponible, abonnement expire) n'empeche pas la creation de la notification in-app. Abonnements retournant statusCode 410 (Gone) ou 404 supprimes automatiquement de la DB (cleanup transparent). configureVapid() lazy : si les variables d'env manquent, sendPushTo* retourne { sent:0, failed:0 } sans crash. | Mai 2026
D-083 | Service worker enregistre aussi sur le dashboard admin : PwaRegister monte dans app/admin/(dashboard)/layout.tsx. Necessaire pour que les admins recoivent des push meme s'ils ne visitent jamais le site public. Le SW de scope / controle l'origine entiere. | Mai 2026
D-084 | Envoi reel campagnes Mail Marketing : POST /api/marketing/campagnes/[id]/envoyer. Sequence : verifier status in [brouillon, planifie], charger contacts (unsubscribed=false, hasSome lists), passer status="en_cours_envoi" + sentAt=now(), boucler par batchs de 50 emails avec Promise.all puis sleep(1000ms) entre batchs (Resend gratuit = 100/jour, Pro = 50000/mois). Pour chaque contact, renderCampaignHtmlWithTracking() resout {{prenom}}/{{nom}}/{{email}} + insere pixel + wrappe les liens. ActivityLog par batch + ActivityLog final + push notification "Campagne envoyee". En cas d'erreur critique : repasser status="brouillon" et sentAt=null. Nouveau statut "en_cours_envoi" ajoute dans CAMPAIGN_STATUSES (label "Envoi en cours", couleur orange). | Mai 2026
D-085 | Mode test campagne : POST /api/marketing/campagnes/[id]/test avec body { email }. Envoie le rendu complet (tracking + pixel + unsubscribe) a une seule adresse de test, sans changer le statut de la campagne ni creer d'evenements CampaignOpen/CampaignClick (le tracking declenche reellement quand l'email est ouvert, mais aucune stat pre-envoi). Subject prefixe "[TEST] ". ActivityLog. Dialog UI dans CampaignDetailView avec input email. | Mai 2026
D-086 | lib/email.ts etendue : sendEmail accepte desormais html?: string en plus de react?: ReactElement. Resend.emails.send supporte les deux modes natifs. Pour les campagnes Mail Marketing (HTML brut depuis renderCampaignHtmlWithTracking), on utilise html. Pour les emails transactionnels existants (React Email templates), on continue avec react. Validation : si ni react ni html, retourne success:false. | Mai 2026
D-087 | Dashboard responsive mobile : sidebar masquee sous lg (hidden lg:flex), ml-[240px] devient lg:ml-[240px] sur le wrapper main. components/admin/bottom-nav.tsx Client Component avec 5 onglets (Accueil/Projets/Clients/Devis/Menu) fixed bottom-0 z-40 lg:hidden, hauteur 64px, fond zinc-900, lien actif en rouge #C8151B (couleur seule, pas de fond). Bouton Menu ouvre un Sheet side=bottom max-h 85vh contenant : handle pill, profil utilisateur, tous les groupes navGroups exportes depuis sidebar.tsx, Parametres + Deconnexion. Fermeture du Sheet au clic sur un lien (onClose). Main : padding p-4 lg:p-6 + pb-20 lg:pb-6 pour eviter occultation par bottom nav. | Mai 2026
D-088 | Topbar responsive : titre text-base lg:text-lg, fil d'Ariane hidden sm:block, bouton action label masque sous sm (icone seule conservee avec aria-label). NotificationsBell conservee a tous les breakpoints. Padding px-4 lg:px-6. | Mai 2026
D-089 | Tableaux dashboard responsive : Shadcn Table contient deja overflow-auto wrapper. Colonnes secondaires masquees via hidden sm:table-cell / hidden md:table-cell / hidden lg:table-cell. clients-table : Type masque sous sm, Canal masque sous md, Projets masque sous sm. projects-table : Type sm, Date md, Lieu lg, Budget md. Colonnes critiques toujours visibles (Nom, Statut, Actions). Autres tables seront adaptees au cas par cas si besoin. | Mai 2026
D-090 | Home page connectee a la DB : Server Component avec revalidate 60s (cohesion D-030). Sources : prisma.trainingSession.findMany(status=ouvert, dateStart>=now, take 2) pour la section Formations, prisma.setting.findMany(about_hero_image + about_stats) pour AboutStatsSection. Fallbacks hardcodes si DB inaccessible ou listes vides. Portfolio reste sur useEffect cote client (deja existant) car deja DB-connected via /api/portfolio. Temoignages : deja DB-connected via /api/parametres (verifie). | Mai 2026
D-091 | AboutStatsSection : image hero configurable via Setting about_hero_image (nouvelle cle ajoutee dans lib/parametres.ts + PARAM_DEFAULTS empty string). Fallback gradient sombre si vide. next/image avec fill + sizes responsive. ImageUpload integre dans Parametres > Contenu (bucket equipe, aspectRatio landscape). Stat headline (carte flottante coin bas-droit) utilise about_stats[0] si renseigne, sinon fallback +200 / evenements diffuses. | Mai 2026
D-092 | FormationsPreviewSection : prop sessions: FormationSession[] optionnelle. Si fournie : mapping via formatDayMonth (jour 2-chiffres + mois court FR Jan/Fev/Mar/...), diffDaysInclusive (Math.round(ms/86400000)+1 jour), formatPriceFCFA. Prix affiche en chip a cote du chip places. Card devient Link href=/formations (toute la carte cliquable). seatsTaken=0 par defaut (count des inscrits confirmes a brancher Phase 4). Si sessions vide : fallback hardcode 2 cartes (Streaming Live + Creator Weekend). | Mai 2026
